⑴요약: 원격 저장소(github)의 레포지토리를 로컬 저장소로 복제

⑵기능: 저장소를 새로 생성된 디렉토리에 복제하고, 복제된 저장소의
              각 브랜치에 대해 원격 추적 브랜치를 생성하며(<code> git
              분기 --remotes 를 사용하여 표시), 현재 활성 상태인 복제된
              저장소에서 포크된 초기 브랜치를 생성하고 이동한다. 복제한 이후,
              인수가 없는 깨끗한 <code> git fetch는 모든 원격 추적 
              브랜치를 업데이트하고, 인수 없는 <code> git pull 은 또한
               원격 마스터 브랜치를 현재 마스터 브랜치로 병합한다("싱글 브랜치"
              가 주어졌을 때는 사실이 아님; 아래참조)
              이 기본 구성은 다음에 대한 대하여 수행된다.
                    ㆍ<code>refs/remotes/origin 아래의 원격 브랜치 헤더에
                       참조 생성<code>remote.origin.origin.url 과 
                    <code>remote.origin.fetch 구성변수를 초기화
                   ㆍ복제할 저장소가 로컬 시스템에 있을 때, 이 플래그는 일반적인 
                    "Git 인식" 전송 메커니즘을 무시하고 HEAD와 객체 아래의 
                          모든 것을 복사하여 저장소를 복제한다.

               <code>.git/objects 디렉터리 아래의 파일은 가능할 때 공간을
              절약하기 위해 강하게 연결된다.

⑶구현: