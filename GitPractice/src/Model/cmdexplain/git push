⑴요약: github 레포지토리에 업로드

⑵기능: 지정된 참조를 완성하는 데 필요한 개체를 전송하면서,
             로컬 참조를 사용하여 원격 참조를 업데이트하십시오. 
             저장소에 hooks를 설치함으로써 저장소에 밀어 넣을
             때마다 흥미로운 일들이 일어나게 할 수 있습니다. 명령어
             라인이 다음 명령어를 사용하여 push할 위치를 지정하지
             않은 경우 현재 브랜치의 <code>레포지토리 인수, <code>
             branch.*.remote 구성은 어디에 push 할 것인지 위치를 결정합니다.
             구성이 누락된 경우 기본값이 원본으로 설정됩니다. 명령어
             라인이나 구성이 무엇을 지정하지 않을 때 push, 기본 동작이
             사용되는데, 이는 <code>push.default로, 현재 브랜치는
             해당 업스트림 브랜치로 밀지만, 안전 대책으로써 업스트림
             브랜치가 로컬 브랜치와 이름이 같지 않으면 푸시 작동이
             중단됩니다. 푸시 작업의 대상인 "원격" 저장소가 있습니다.
             이 매개 변수는 URL ([a href="#URLS"]아래 GIT URLS 섹션 참조)
             또는 리모트 이름([a href="#REMOTEs"#REMOTES)일 수 있습니다.
             소스 객체로 업데이트할 대상 참조를 지정해야 합니다.

⑶구현: