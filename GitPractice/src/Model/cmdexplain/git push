⑴요약: 로컬 저장소에서 push한 내용을 원격 저장소 github 레포지토리에 업로드

⑵기능: 이 명령어는 마지막으로 커밋한 사항을 git 레포지토리에 올립니다. push가
             되지 않으면 원격 서버에 변경사항이 저장되지 않습니다. 이 명령은 git push
             [리모트 저장소 이름] [브랜치 이름]의 형태를 띕니다. 아무것도 작성하지
             않았다면 기본값(origin master)이 자동으로 설정됩니다. 현재 작업하던
             브랜치가 자신을 업스트림 브랜치로 push하지만, 안전 대책으로써 업스트림
             브랜치가 로컬 브랜치와 이름이 같지 않으면 push 작동이 중단됩니다.  

⑶구현: 'git push'만 입력했을 때 --set-upstream 옵션을 이용하여
              특정 브랜치의 upstream을 설정해 준 경우, 자동으로 연결된 upstream
              으로 업로드가 되지만 이 프로그램은 연습을 목적으로 하기에 반드시 git push
              명령어에 리모트와 브랜치를 명시해주어야합니다.