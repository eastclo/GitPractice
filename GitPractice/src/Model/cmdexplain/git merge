⑴요약: 두 브랜치를 병합함

⑵기능: 이 명령어는 두 개의 브랜치를 병합시켜 하나의 브랜치로 작업할 수 
              있게끔 합니다. 'master'브랜치에 다른 브랜치(편의상 '대상
              브랜치'라고 칭함)을 넣기 위해서는 우선 'master'브랜치에 HEAD가
              위치하게 만들어야 합니다. 두 브랜치가 작업하던 파일이 변경되지
              않았다면 'master'브랜치를 가리키는 커밋은 대상 브랜치와 같은
              위치로 이동합니다.이런 방식의 병합을 'fast-forward 병합'
              이라고 합니다. 이는 대상  master브랜치의 커밋을 대상 브랜치
              커밋으로 이동하겠다는 의미입니다. 그런데 master브랜치와 대상
              브랜치가 같은 파일의 같은 부분을 수정하면 충돌이 발생합니다.
              이 경우 상황은 다음과 같습니다.
               ㆍmaster 브랜치와 대상 브랜치의 커밋 수가 같습니다.
               ㆍmaster 브랜치와 대상브랜치는 파일의 내용을 각각 다른
              값으로 동시에 수정했습니다.
              따라서 Git은 사용자에게 어떤 브랜치의 내용을 선택할 것인지 제시합니다.
              'master'브랜치에서 충돌이 일어난 파일을 열어 <<<<HEAD와
              >>>>>대상브랜치명   사이, ====== 를 수정해야 합니다.

⑶구현: 'git merge master 브랜치1'를 실행할 때, 같은 파일을 서로 다르게
              작업했다면 발생하는 충돌을 수정해주어야 합니다. 사용자는 파일을 직접 수정
              하여 둘 중 원하는 파일을 선택해야 합니다. 수정이 완료되면 master 폴더에
               변경된 파일이 새로 저장 됩니다.